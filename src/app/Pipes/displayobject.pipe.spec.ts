import { DisplayobjectPipe } from './displayobject.pipe';

describe('DisplayobjectPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayobjectPipe();
    expect(pipe).toBeTruthy();
  });
});
